/**
 * Analytics IPC handlers — NOT YET IMPLEMENTED.
 *
 * Column counts are handled by registerBoardHandlers (ipc/boards.ts) because
 * they are a basic board-data query, not a derived analytic.
 *
 * All advanced analytics (closed per week, by label, card age) are documented
 * in docs/analytics/REQUIREMENTS.md for a future agent to implement.
 */

export {}
